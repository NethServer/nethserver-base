{
    my $isdn = $isdn{'status'} || "disabled";
    my $sync = $isdn{'UseSyncPPP'} || "yes";
    $OUT .= ($isdn eq "enabled" && $sync eq "yes") ?
		"mode dev" : "mode ppp";
}
modem
lock
crtscts
dynamic
defaultroute
