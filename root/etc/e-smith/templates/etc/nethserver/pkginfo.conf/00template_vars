{
    #
    # 00template_vars
    #
    our @repo_list = qw(
            nethserver-updates
            nethforge
        );
    if ($subscription{'ExtraRepositories'}) {
        my @ExtraRepositories = split(',',$subscription{'ExtraRepositories'});
        push (@repo_list, @ExtraRepositories);
    }
    '';
}
